<nav id="navbar" class="navbar order-last order-lg-0">
    <ul>
        <li><a class="nav-link scrollto " href="{{ path('home') }}">Accueil</a></li>
        
        {% if not is_granted("ROLE_USER") %}
            <li><a class="nav-link scrollto" href="{{ path('app_register') }}">Enregistrement</a></li>
            <li><a class="nav-link scrollto" href="{{ path('app_login') }}">Connexion</a></li>
        {% else %}
            <li><a class="nav-link scrollto" href="{{ path('speciality_index') }}">Les Pratiques</a></li>
            <li class="dropdown"><a class="nav-link scrollto" href="#"><span>Ressources</span> <i class="bi bi-chevron-down"></i></a>
                <ul>
                <li><a class="nav-link scrollto" href="{{ path('article_index') }}">Articles</a></li>
                <li><a class="nav-link scrollto" href="{{ path('document_index') }}">Documents</a></li>
                <li><a class="nav-link scrollto" href="{{ path('webinar_index') }}">Webinars</a></li>
                </ul>
            </li>
            <li class="dropdown"><a class="nav-link scrollto" href="#"><span>Mon Compte</span> <i class="bi bi-chevron-down"></i></a>
                <ul>
                    <li><a class="nav-link scrollto" href="{{ path('user_profile') }}">Mon Profile</a></li>
                    {% if (is_granted("ROLE_DOC") or is_granted("ROLE_PRO")) %}
                        <li><a class="nav-link scrollto" href="{{ path('appointment_create') }}">Ajouter des disponibilités</a></li>
                        <li><a class="nav-link scrollto" href="{{ path('appointment_index') }}">Mes rendez-vous / Mes Disponibilités</a></li>
                    {% endif %}
                    {% if (is_granted("ROLE_PATIENT")) %}
                        <li><a class="nav-link scrollto" href="{{ path('appointment_index') }}">Mes rendez-vous</a></li>
                    {% endif %}
                    <li><a class="nav-link scrollto" href="{{ path('app_logout') }}">Deconnexion</a></li>
                </ul>
            </li>
            {% if is_granted("ROLE_ADMIN") %}
                <li><a class="nav-link scrollto" href="{{ path('admin_home') }}">Administration</a></li>
            {% endif %}
        {% endif %}

        <li><a class="nav-link scrollto" href="{{ path('contact') }}">Contact</a></li>

    </ul>
    <i class="bi bi-list mobile-nav-toggle"></i>
</nav><!-- .navbar -->
