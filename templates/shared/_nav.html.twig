<nav id="navbar" class="navbar order-last order-lg-0">
    <ul>
    <li><a class="nav-link scrollto" href="{{ path('home') }}">Accueil</a></li>
    
    {% if not is_granted("ROLE_USER") %}
        <li><a class="nav-link scrollto" href="{{ path('app_register') }}">Enregistrement</a></li>
        <li><a class="nav-link scrollto" href="{{ path('app_login') }}">Connexion</a></li>
    {% else %}
        {# <li><a class="nav-link scrollto" href="{{ path('user_index') }}">Mon Compte</a></li> #}
        <li><a href="{{ path('speciality_index') }}">Les Pratiques</a></li>
        <li class="dropdown"><a href="#"><span>Mon Compte</span> <i class="bi bi-chevron-down"></i></a>
            <ul>
              <li><a href="{{ path('user_index') }}">Mon Profile</a></li>
                {% if (is_granted("ROLE_DOC") or is_granted("ROLE_DOC")) %}
                    <li><a class="nav-link scrollto" href="{{ path('user_disponibility') }}">Mes disponibilités</a></li>
                {% endif %}
                {% if (is_granted("ROLE_PATIENT")) %}
                    <li><a class="nav-link scrollto" href="{{ path('user_reservation') }}">Mes rendez-vous</a></li>
                {% endif %}
            </ul>
        </li>
        <li class="dropdown"><a href="#"><span>Ressources</span> <i class="bi bi-chevron-down"></i></a>
            <ul>
              <li><a href="{{ path('article_index') }}">Articles</a></li>
              <li><a href="{{ path('document_index') }}">Documents</a></li>
              <li><a href="{{ path('webinar_index') }}">Webinars</a></li>
            </ul>
        </li>
        {% if is_granted("ROLE_ADMIN") %}
            <li><a class="nav-link scrollto" href="{{ path('admin_home') }}">Administration</a></li>
        {% endif %}
        <li><a class="nav-link scrollto" href="{{ path('app_logout') }}">Deconnexion</a></li>
    {% endif %}

    </ul>
    <i class="bi bi-list mobile-nav-toggle"></i>
</nav><!-- .navbar -->
