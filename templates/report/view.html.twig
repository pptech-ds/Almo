{% extends 'base.html.twig' %}

{% block title %}compte-rendu{% endblock %}

{% block body %}
    <div class="container">

        <div style="height: 10rem">
        </div>
        
        <!-- ======= Breadcrumbs Section ======= -->
        <section class="breadcrumbs">

            <div class="d-flex justify-content-between align-items-center">
                <h2 class="fw-bold">{{report.name}}</h2>
                <ol>
                    <li><a href="{{path('appointment_index')}}">Retour à ma liste de RDV</a></li>
                    <li>{{report.name}}</li>
                </ol>
            </div>

        </section><!-- End Breadcrumbs Section -->

        <section class="inner-page">
            <p style="height: 100%; ">{{report.reportPatient | raw}} </p>
            <p class="fs-6">Ecrit par {{report.createdBy.civility}} {{report.createdBy.firstname}} {{report.createdBy.lastname}}</p>
        </section>

    </div>
{% endblock %}