{% extends 'base.html.twig' %}

{% block title %}Les disponibilités{% endblock %}

{% block body %}

    <div class="container">

        <div style="height: 10rem">
        </div>

        {# {{ dump(reservation) }} #}
        <!-- ======= Breadcrumbs Section ======= -->
        <section class="breadcrumbs">
            <div class="d-flex justify-content-between align-items-center">
                <h2>{{reservation.name}}</h2>
                <ol>
                    <li><a href="{{path('appointment_index')}}">Retour à ma liste de RDV</a></li>
                    <li>{{reservation.name}}</li>
                </ol>
            </div>
        </section><!-- End Breadcrumbs Section -->

        <section class="inner-page">
        
            <p>{{reservation.content | raw}} </p>
            <p class="fs-6">Créée par <a href="{{path('user_biography', {'id':reservation.createdBy.id})}}">{{ reservation.createdBy.civility }} {{ reservation.createdBy.firstname }} {{ reservation.createdBy.lastname }}</a></p>
       
        </section>

    </div>


{% endblock %}


{% block javascripts %}
{% endblock %}