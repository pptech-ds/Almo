{% extends 'admin/sbadmin2.html.twig' %}

{% block title %}Ajout d'un utilisateur{% endblock %}

{% block body %}
    <div class="container-fluid">

        <!-- Page Heading -->
        <h1 class="h3 mb-2 text-gray-800">Ajouter un utilisateur</h1>

        <!-- DataTales Example -->
        <div class="card shadow mb-4">
            <div class="card-header py-3">
                <a href="{{ path('admin_user_add_patient') }}" class="btn btn-primary btn-icon-split btn-lg">
                    <span class="icon text-white-50"> 
                        <i class="fas fa-user-injured"></i>
                    </span>
                    <span class="text">Ajouter un patient</span>
                </a>
                <a href="{{ path('admin_user_add_doc') }}" class="btn btn-success btn-icon-split btn-lg">
                    <span class="icon text-white-50">
                        <i class="fas fa-user-md"></i>
                    </span>
                    <span class="text">Ajouter un médecin</span>
                </a>
                <a href="{{ path('admin_user_add_pro') }}" class="btn btn-info btn-icon-split btn-lg">
                    <span class="icon text-white-50">
                        <i class="fas fa-user-nurse"></i>
                    </span>
                    <span class="text">Ajouter un profesionnel</span>
                </a>
                <a href="{{ path('admin_user_add_admin') }}" class="btn btn-danger btn-icon-split btn-lg">
                    <span class="icon text-white-50">
                        <i class="fas fa-user-cog"></i>
                    </span>
                    <span class="text">Ajouter un administrateur</span>
                </a>
            </div>
                
        </div>

    </div>
    <!-- /.container-fluid -->
{% endblock %}



{% block javascripts %}

{% endblock %}
    